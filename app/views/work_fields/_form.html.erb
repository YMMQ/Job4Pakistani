<%= form_for(@work_field) do |f| %>
  <% if @work_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_field.errors.count, "error") %> prohibited this work_field from being saved:</h2>

      <ul>
      <% @work_field.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :field_name %><br>
    <%= f.text_field :field_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
